# Analysis-side dependencies for reproducing the DPE tables/figures from the
# result DBs. (GPU generation on the cluster uses the separate `fingerprint` /
# `internvl` conda envs — see docs/REPRODUCE_DPE.md.)
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dpe.txt
#
# Versions are those used to produce the paper artifacts; >= is usually fine.
numpy==2.1.3
pandas==2.3.2
matplotlib==3.10.8
scipy==1.15.3
torch==2.9.1                 # only needed for `norms` (loads the DPE encoder) and the embedding dump
sentence-transformers==5.6.0 # only needed for `content` (SBERT response similarity)
umap-learn==0.5.11           # only needed for `umap` (visual-token embedding UMAP)
# sqlite3 is in the Python standard library.
